/// 播放列表重复模式枚举
/// 避免与Flutter框架的RepeatMode冲突，使用更具业务语义的名称
enum PlaylistRepeatMode {
  /// 正常播放：按顺序播放，播放完最后一首后停止
  off,
  
  /// 列表循环：按顺序播放，播放完最后一首后回到第一首继续
  all,
  
  /// 单曲循环：重复播放当前歌曲
  one,
  
  /// 随机播放：随机选择下一首歌曲
  shuffle,
}
